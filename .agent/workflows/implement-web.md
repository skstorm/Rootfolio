---
description: Web Implementation Expert - Writes actual code and verifies behavior.
---

This workflow builds actual functional code based on the planning and design.

1. **Technical Environment Setup**: Perform initial Vite project setup and create the basic folder structure (e.g., `platform/`).
2. **Core Implementation**:
    - Build the core layout and navigation engine using HTML/CSS/JS.
    - Apply responsive design to ensure perfection on both mobile and desktop.
3. **Content Engine Construction**: Implement functionality to read Markdown files and render them dynamically on the web page.
// turbo
4. **Autonomous Verification**: Call `browser_subagent` to test if the implemented pages work as intended and immediately fix errors upon discovery.
5. **Completion Report**: Report the final results through `walkthrough.md`.
